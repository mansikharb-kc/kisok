import Placeholder from "@/components/Placeholder";

export default function Page() {
  return <Placeholder title="Warehouse & Locations" note="Branch Admin: build the nested location tree (Warehouse → Blocks → Racks → Tray → Custom)." />;
}
