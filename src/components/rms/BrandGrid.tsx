// BBB/BBP — brand tiles -> products; sponsored / featured / most-viewed / most-added-to-BOM. Phase: P1.5
// TODO: brand cards, brand page (categories + featured), product list.
export default function BrandGrid({ token }: { token: string }) {
  return <section className="p-8">Brands — scaffold (Phase 1.5) · screen {token}</section>;
}
