// BBC — category tiles + drill to sub-category -> products. Phase: P1.3
// TODO: category tree/chips with counts, search, route to products.
export default function CategoryGrid({ token }: { token: string }) {
  return <section className="p-8">Categories — scaffold (Phase 1.3) · screen {token}</section>;
}
