// BOM cart — list items, customer capture, QR phone handoff, email/CRM as quote (no price). Phase: P1.6 / P1.10
// TODO: GET/POST /api/rms/bom; CustomerCapture; generate quote.
export default function BomCart({ token }: { token: string }) {
  return <section className="p-8">BOM — scaffold (Phase 1.6) · screen {token}</section>;
}
