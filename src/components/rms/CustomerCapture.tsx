// Customer identification — scan/loyalty/lead capture; ties BOM to a customer. Phase: P1.10
// TODO: capture customer (scan/phone/lead), attach to bom_list, QR handoff to phone.
export default function CustomerCapture() {
  return <div>{/* scaffold — Phase 1.10 */}</div>;
}
