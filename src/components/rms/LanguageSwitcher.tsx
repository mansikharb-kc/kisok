// Language switcher — multi-language kiosk (ties to lib/rms/i18n). Phase: P1.9
// TODO: list SUPPORTED_LOCALES, switch locale, RTL handling.
export default function LanguageSwitcher() {
  return <div>{/* scaffold — Phase 1.9 */}</div>;
}
